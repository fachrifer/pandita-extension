// config.js — Single source of truth for Pandita configuration
// The BASE_URL is NEVER user-configurable. Change it here only.
const CONFIG = {
  BASE_URL: "https://ai.kemenkeu.go.id",
  APP_NAME: "Pandita",
  APP_DESCRIPTION: "Bagian dari Ekosistem Kemenkeu AI Platform",
  MAX_PAGE_CONTENT_LENGTH: 15000,
  AUTH_POLL_INTERVAL_MS: 2000,
  AUTH_POLL_MAX_ATTEMPTS: 60,
  MAX_CHAT_SLOTS: 3,
  CHAT_REFRESH_INTERVAL_MS: 30000
};
